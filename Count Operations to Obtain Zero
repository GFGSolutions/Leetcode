// User template for Java

class Solution {
  public int countOperations(int num1, int num2) {
    long a = num1, b = num2;
    int ops = 0;
    while (a > 0 && b > 0) {
      if (a < b) { long tmp = a; a = b; b = tmp; }
      ops += (int)(a / b);
      a %= b;
    }
    return ops;
  }
}
